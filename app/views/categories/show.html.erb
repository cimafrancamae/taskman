<%= link_to "Edit", edit_category_path %>
<%= button_to "Delete", category_path, method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this category?" } %>

<%= render 'tasks/index', category: @category %>