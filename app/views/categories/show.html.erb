<%
=begin%>
 <%= link_to "Edit", edit_category_path(@category), class: "bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-700 mr-2" %>

<%= button_to "Delete", category_path(@category), method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this category?" }, class: "bg-red-500 text-white py-2 px-4 rounded hover:bg-red-700" %>

<%= render 'tasks/index', category: @category %> 
<%
=end%>


<%= render partial: 'category', locals: { category: @category } %>

<div class="flex space-x-2">
  <%= link_to "Edit", edit_category_path(@category), class: "bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-700" %>
  <%= button_to "Delete", category_path(@category), method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this task?" }, class: "bg-red-500 text-white py-2 px-4 rounded hover:bg-red-700" %>
</div>

